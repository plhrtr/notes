import { createComponent } from "@lit/react";
import { MdList as _MdList } from "@material/web/list/list.js";
import React from "react";

export const MdList = createComponent({
  tagName: "md-list",
  elementClass: _MdList,
  react: React,
});
